{# {% extends 'emails/base.html.twig' %}

{% block body %}
    <h1>Bonjour
        {{ user.firstname }}
        {{ user.lastname }}</h1>

    <p>Votre compte a été activé sur le site du club des Belles Images.</p>
    <p>Dès maintenant, vous pouvez vous connecter avec l'adresse-mail que vous avez définie ({{ user.email}}) ou votre nom d'utilisateur ({{ user.username }}) et le mot de passe que vous avez défini lors de la création de votre compte.</p>
    <p>
        <a href="{{ path("app_login") }}" target="_blank" rel="noopener noreferrer">Se connecter</a>
    </p>

    <img src="{{ email.image("@images/logo-cbi.jpg") }}" width="200" alt="Logo cbi">
{% endblock %} #}


{% apply inky_to_html|inline_css(source('@styles/foundation-emails.css'))|inline_css %}
<style>
	body {
		font-family: "Comic Sans MS";
		font-weight: normal;
	}

	.font-bold {
		font-weight: bold;
	}

	.logo {
		width: 150px;
	}
</style>

<container>
	<row class="header">
		<columns>
			<spacer size="16"></spacer>
			<h1 class="font-bold">
				Bonjour
				{{ user.firstname }}
				{{ user.lastname }},
			</h1>
		</columns>
	</row>
	<row>
		<columns>
			<p>Votre compte a été activé sur le site du club des Belles Images.</p>
			<p>Dès maintenant, vous pouvez vous connecter avec l'adresse-mail que vous avez définie (<span class="font-bold">{{ user.email}}</span>) ou votre nom d'utilisateur (<span class="font-bold">{{ user.username }}</span>) et le mot de passe que vous avez défini lors de la création de votre compte.</p>
		</columns>
	</row>
	<row>
		<columns>
			<center>
				<button href="{{ url("app_login") }}">Se connecter</button>
			</center>
		</columns>
	</row>
	<row>
		<columns>
			<center>
				<img src="{{ email.image("@images/logo-cbi.jpg") }}" class="logo" width="200" alt="Logo cbi">
			</center>
		</columns>
	</row>
</container>
{% endapply %}
