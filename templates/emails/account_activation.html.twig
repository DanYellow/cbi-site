{% set content %}
{% include 'emails/style.html.twig' %}
{% endset %}


{% apply inky_to_html|inline_css(source('@styles/foundation-emails.css'))|inline_css %}
{{ content }}
<container class="body">
	<row class="header">
		<columns>
			<spacer size="16"></spacer>
			<h1 class="font-bold">
				Bonjour
				{{ user.firstname }}
				{{ user.lastname }},
			</h1>
		</columns>
	</row>
	<row>
		<columns small="6" large="4">
			<p>Votre compte a été activé sur le site web du Club des Belles Images.</p>
			<p>Dès maintenant, vous pouvez vous connecter avec l'adresse-mail que vous avez définie (<span class="font-bold">{{ user.email}}</span>) ou votre nom d'utilisateur (<span class="font-bold">{{ user.username }}</span>) et le mot de passe que vous avez défini lors de la création de votre compte.</p>
		</columns>
	</row>
	<row>
		<columns>
			<center>
				<button class="expanded rounded center" href="{{ url("app_login") }}">Se connecter</button>
			</center>
		</columns>
	</row>
	<row>
		<columns>
			<center>
				<img src="{{ email.image("@images/logo-cbi.jpg") }}" class="logo" width="200" alt="Logo cbi">
			</center>
		</columns>
	</row>
	<row>
		<columns>
			<center>
				<p>
					<small class="small">Réunions tous les lundis (sauf jours fériés et vacances scolaires) à 21h</small>
				</p>
			</center>
		</columns>
	</row>
</container>
{% endapply %}
